[package]
name = "sandbox-runtime"
version = "0.1.0"
edition = "2021"
description = "A standalone Rust sandbox tool using Landlock + seccomp for filesystem and syscall isolation"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
dirs = "6"
libc = "0.2"
landlock = "0.4"

[dev-dependencies]
tempfile = "3"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
